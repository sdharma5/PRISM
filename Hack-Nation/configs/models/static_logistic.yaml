# L2 logistic regression over the static feature matrix — the reference model.
model:
  name: static_logistic
  class: models.tabular.logistic.LogisticStaticModel
  params:
    C: 1.0
    penalty: l2
    solver: lbfgs
    max_iter: 2000
    class_weight: balanced
    threshold: 0.5
